{{- if and .Values.planes.compute.enabled .Values.compute.workloadNamespace.create }}
# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.compute.workloadNamespace.name | quote }}
  labels:
    {{- include "osmo.component.labels" (dict "root" . "component" "workload-namespace") | nindent 4 }}
  annotations:
    helm.sh/resource-policy: "keep"
{{- end }}
