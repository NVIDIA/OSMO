# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

externalUrl: https://public-control.example.com

imageRegistry: registry.example.com/osmo
imageTag: review
imagePullSecrets:
- name: compute-pull-secret

commonLabels:
  example.com/common: compute
commonAnnotations:
  example.com/common-annotation: compute
podDefaults:
  labels:
    example.com/pod-default: compute
  annotations:
    example.com/pod-annotation: compute
  podSecurityContext:
    fsGroupChangePolicy: OnRootMismatch
  nodeSelector:
    example.com/node-pool: compute

compute:
  backendName: custom
  workloadNamespace: custom-workflows
  # Intentionally unused because the backend test runner is disabled below.
  backendTestNamespace: disabled-backend-tests
  authentication:
    existingSecret: custom-backend-token
    tokenKey: token

services:
  backendListener:
    image:
      repository: custom/backend-listener
    serviceAccount:
      name: custom-listener
      create: true
    extraEnv:
    - name: CUSTOM_LISTENER
      value: enabled
    extraArgs:
    - --include_namespace_usage
    - custom-workflows
    pod:
      annotations:
        example.com/listener-annotation: custom
  backendWorker:
    image:
      repository: custom/backend-worker
    serviceAccount:
      name: custom-worker
      create: true
  backendTestRunner:
    enabled: false
