# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# SPDX-License-Identifier: Apache-2.0

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.router.serviceName }}
  labels:
    app: {{ .Values.services.router.serviceName }}
spec:
  replicas: {{ .Values.services.router.scaling.minReplicas }}
  selector:
    matchLabels:
      app: {{ .Values.services.router.serviceName }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.router.serviceName }}
        {{- with .Values.services.router.extraPodLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        {{- with .Values.services.router.extraPodAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.services.router.hostAliases }}
      hostAliases:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.services.router.topologySpreadConstraints }}
      topologySpreadConstraints:
      {{- range . }}
      - topologyKey: {{.topologyKey }}
        {{- range $key, $value := . }}
        {{- if ne $key "topologyKey" }}
        {{$key}}: {{$value}}
        {{- end }}
        {{- end }}
        labelSelector:
          matchLabels:
            app: {{ $.Values.services.router.serviceName }}
      {{- end }}
      {{- end }}
      nodeSelector:
        {{- range $k, $v := .Values.global.nodeSelector }}
        {{ $k }}: {{ $v }}
        {{- end }}
        {{- range $k, $v := .Values.services.router.nodeSelector }}
        {{ $k }}: {{ $v }}
        {{- end }}
      {{- with .Values.services.router.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.global.imagePullSecret }}
      imagePullSecrets:
      - name: {{ .Values.global.imagePullSecret }}
      {{- end }}
      serviceAccountName: {{ include "osmo.service-account-name" (dict "serviceAccountName" .Values.services.router.serviceAccountName "Values" .Values) }}
      {{- with .Values.services.router.initContainers }}
      initContainers:
      {{- toYaml . | nindent 6 }}
      {{- end }}
      containers:
      - name: {{ .Values.services.router.serviceName }}
        image: {{ .Values.global.osmoImageLocation }}/{{ .Values.services.router.imageName }}:{{ .Values.services.router.imageTag | default .Values.global.osmoImageTag }}
        command:
        - router
        args:
        {{- if .Values.services.router.hostname }}
        - --hostname
        - {{ .Values.services.router.hostname }}
        {{- end }}
        - --postgres_host
        - {{ .Values.services.postgres.serviceName }}
        - --postgres_port
        - {{ .Values.services.postgres.port | quote }}
        - --postgres_database_name
        - {{ .Values.services.postgres.db}}
        {{- if .Values.services.postgres.user }}
        - --postgres_user
        - {{ .Values.services.postgres.user }}
        {{- end }}
        {{- if .Values.services.configFile.enabled }}
        - --mek_file
        - {{ .Values.services.configFile.path }}
        {{- else }}
        - --config
        - {{ .Values.services.configFile.path }}
        {{- end }}
        {{- if .Values.global.logs.enabled }}
        - --log_level
        - {{ .Values.global.logs.logLevel }}
        - --k8s_log_level
        - {{ .Values.global.logs.k8sLogLevel }}
        - --log_dir
        - /logs
        - --log_name
        - router_service
        {{- end }}
        {{- with .Values.services.router.extraArgs }}
        {{- range . }}
        - {{ . }}
        {{- end }}
        {{- end }}
        env:
        - name: OSMO_SCHEMA_VERSION
          value: {{ .Values.targetSchema | default "public" }}
        {{- if .Values.services.postgres.password }}
        - name: OSMO_POSTGRES_PASSWORD
          value: {{ .Values.services.postgres.password }}
        {{- else if .Values.services.configFile.enabled }}
        - name: OSMO_POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-secret
              key: db-password
        {{- end }}
        {{- with .Values.services.router.extraEnvs }}
        {{- range . }}
        - name: {{ .name }}
          {{- if .value }}
          value: {{ .value | quote }}
          {{- else if .valueFrom }}
          valueFrom:
            {{- toYaml .valueFrom | nindent 12 }}
          {{- end }}
        {{- end }}
        {{- end }}
        imagePullPolicy: {{ .Values.services.router.imagePullPolicy }}
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
          runAsNonRoot: true
          runAsUser: 1001
        ports:
        - name: http
          containerPort: 8000
          protocol: TCP
        {{- with .Values.services.router.extraPorts }}
        {{- range . }}
        - name: {{ .name }}
          containerPort: {{ .containerPort }}
          protocol: {{ .protocol | default "TCP" }}
        {{- end }}
        {{- end }}
        {{- if or .Values.global.logs.enabled .Values.services.configFile.enabled .Values.services.router.extraVolumeMounts }}
        volumeMounts:
        {{- if .Values.services.configFile.enabled}}
        - mountPath: {{ .Values.services.configFile.path }}
          name: mek-volume
          subPath: mek.yaml
        {{- end }}
        {{- if .Values.global.logs.enabled }}
        - name: logs
          mountPath: /logs
        {{- end }}
        {{- with .Values.services.router.extraVolumeMounts }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
        resources:
        {{- toYaml .Values.services.router.resources | nindent 10 }}

        {{- with .Values.services.router.livenessProbe }}
        livenessProbe:
          {{- toYaml . | nindent 10 }}
        {{- end }}

        {{- with .Values.services.router.startupProbe }}
        startupProbe:
          {{- toYaml . | nindent 10 }}
        {{- end }}

        {{- with .Values.services.router.readinessProbe }}
        readinessProbe:
          {{- toYaml . | nindent 10 }}
        {{- end }}

      {{- with .Values.services.router.extraContainers }}
      {{- toYaml . | nindent 6 }}
      {{- end }}
      volumes:
        {{- if .Values.global.logs.enabled }}
        - name: logs
          emptyDir: {}
        {{- end}}
        {{- if .Values.services.configFile.enabled}}
        - configMap:
            defaultMode: 420
            items:
            - key: mek.yaml
              path: mek.yaml
            name: mek-config
          name: mek-volume
        {{- end}}
        {{- with .Values.services.router.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}

---

apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.router.serviceName }}
  labels:
    app: {{ .Values.services.router.serviceName }}
spec:
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.services.router.serviceName }}

---

apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.router.serviceName }}-headless
  labels:
    app: {{ .Values.services.router.serviceName }}
spec:
  clusterIP: None
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.services.router.serviceName }}

---

apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.services.router.serviceName }}
  labels:
    app: {{ .Values.services.router.serviceName }}
  annotations:
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.services.router.serviceName }}
  minReplicas: {{ .Values.services.router.scaling.minReplicas }}
  maxReplicas: {{ .Values.services.router.scaling.maxReplicas }}
  metrics:
  - type: ContainerResource
    containerResource:
      name: memory
      container: {{ .Values.services.router.serviceName }}
      target:
        type: Utilization
        averageUtilization: {{ .Values.services.router.scaling.memoryTarget }}
  - type: ContainerResource
    containerResource:
      name: cpu
      container: {{ .Values.services.router.serviceName }}
      target:
        type: Utilization
        averageUtilization: {{ .Values.services.router.scaling.hpaCpuTarget }}
  {{- with .Values.services.router.scaling.customMetrics }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
