# OSMO Router Values - Auto-generated
global:
  osmoImageLocation: nvcr.io/nvidia/osmo
  osmoImageTag: latest

services:
  configFile:
    enabled: true

  service:
    scaling:
      minReplicas: 1
      maxReplicas: 1
    ingress:
      enabled: false

  postgres:
    serviceName: osmo-cluster-postgres.postgres.database.azure.com
    port: 5432
    db: osmo
    user: postgres
    passwordSecretName: db-secret
    passwordSecretKey: db-password

sidecars:
  otel:
    enabled: false
  envoy:
    enabled: false
  logAgent:
    enabled: false
