# OSMO Web UI Values - Auto-generated
global:
  osmoImageLocation: nvcr.io/nvidia/osmo
  osmoImageTag: latest

services:
  ui:
    replicas: 1
    hostname: "osmo-minimal.local"
    apiHostname: "osmo-service.osmo-minimal.svc.cluster.local:80"
    ingress:
      enabled: false

sidecars:
  envoy:
    enabled: false
  logAgent:
    enabled: false
