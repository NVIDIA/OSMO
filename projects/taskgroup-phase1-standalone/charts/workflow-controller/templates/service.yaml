{{- if .Values.metrics.service.create -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "workflow-controller.fullname" . }}-metrics
  labels:
    {{- include "workflow-controller.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - name: metrics
    port: {{ .Values.metrics.service.port }}
    targetPort: metrics
  selector:
    {{- include "workflow-controller.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: workflow-controller
{{- end }}
