workflow:
  name: gr00t-app-run
  tasks:
  - name: train
    image: nvcr.io/nvidia/gr00t:1.5.0
    command: ["bash", "/tmp/entry.sh"]
  resources:
    default:
      cpu: 32
      gpu: 4
      memory: 256Gi
      storage: 500Gi
default-values:
  model: gr00t-base
